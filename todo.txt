todo:
* in app.js handle this better: hint.innerText = "FIXME: unhandled type '"+type+"', fallback to:String value";
* empty password in js/html should retain prev. value, which currently is "********" (no quotes). Unclear why/how even if it's kept at the default of "********" it will still not be set to this (wrong)value when submitting changes for other keys, which is the correct way so it's already doing it somehow. I forget how app.js actually does it.
* ensure the new webUI entered password when set, asks for confirmation of it before continuing to set it.
* when saving config from webUI and the on-disk config was changed, what do we do exactly? if it's same it's np, but if anything changed, which should take prio then? the webUI page coulda been more stale than the more recent on-disk config.json change, heck maybe the change wasn't even reloaded via Ctrl+R yet!
* find other uses of hardcoded json tags like "webui_password_hash" is/was., there are some in app.js like 'block_mode' etc, unclear how to template these into app.js which is static currently! maybe read these in from the html? this way templating on the html works.
* DRY uses of "webui_password_hash" and ensure we use the json tag of it, because if renamed in the Config struct, uses of hardcoded "webui_password_hash" will fail miserably otherwise. This affects app.js too, for it uses 'webui_password_hash' and there's more like 'block_mode' etc.
* look for TODO and FIXME (not finished ones like doneTODO or nvmFIXME) and try to figure out how to fix.
* fixes/improvements for v0.10.0 (done:1_1, 3_1, 1_2, 1_3, 3_2, 4_2  todo: 4_1,  dontdo: 2): https://gemini.google.com/app/8bc2956c9fed71bb
* improvements for v0.0.6 (did 1,2,4,5,6; todo: 3) : https://gemini.google.com/app/8fba2ee635340715
* do more encapsulation for v0.1.1 (did ) : https://gemini.google.com/app/573dd1cd10f69c76
* link to Tinywall in readme and to Go lang to install Go.

no:
* make Edit button work without javascript? in webUI; Add/Delete already do work tho. This is too much work, not gonna do it because I'm not gonna use the program anyway since switching to Linux where I won't need it in this form.


done:
//* append the exe that made the query for the specific hostname to the nextdns upstream url so the exe appears in nextdns' logs! like in DoH Path only as: "{builtin:clientexe}"
//* if an upstream_urls line (aka url) is too horizontally-long, it wraps into the next line, need a way to visually see what constitutes a line, maybe each line should be zebra colored (background color different) ? but we definitely should keep wrapping it, i think, else it would go too much to the right and make editing weird visually when moving cursor (hides the left/prefix of everything after aka the next lines go out of visual, I think, haven't tried w/o wrap)
//* mention the upstream_urls template {builtin:clientexe} in the /config webUI page's notice and include the key name upstream_utls dynamically, not hardcode it (in case we rename it in .go code) - probably has to be sent via template var in configHandler akin to how "Fields" is set/sent.
//* if i horiz. resize browser window, the webUI has fixed size, it won't expand to fit it, it should, maybe also have a minimum size as well, for when shrank too much.
//* find more places where I need to be Replacing Type Assertions with errors.As
//* find more places where I should be using errors.Is (no warnings about it currently tho)
//* in /config webUI page, prevent the row(s) from shrinking when entering Edit mode, their expansion is already prevented (except for textarea, which is intended)
//* in /config webUI page, remember the last textarea height size(not the width too which should be fixed, depending on column anyway) (clamp to minimum, unless it's done automatically anyway) and use that on any new/future uses of that textarea, for values(doh) only. Not sure if it makes sense to remember individual textarea sizes, just one and apply to all seems enough? For ideas see: https://chatgpt.com/g/g-p-69ef97a8577081919d895e95cb68f22d-dnsbollocks/c/6a4a58ca-0c00-83eb-b5c7-25a835647ca2
//* in /config at least, if not in others too, can we get some text highlight on what the filter matched ? but not just the first one but all occurrences, especially important in description which may have say multiple "upstream" text in it and all should be highlighted if filter is "upstream". For ideas see: https://chatgpt.com/g/g-p-69ef97a8577081919d895e95cb68f22d-dnsbollocks/c/6a4a5b17-7e24-83eb-b833-f58e01d9b7e5?tab=sources
//* get the textarea in /config to be at least as big as the shown items in the value ie. if 4 lines because there are 4 upstream_urls then textarea should be at least 4.5(to see if a next line is there) or 5 lines long when Edit gets clicked first time, currently it's like 3.5 lines long.
//* on /config webUI page, during Edit the row vertical size changes, it shouldn't! it either expands or shrinks currently. The expand is ok for textarea ones, even though they could also be precomputed maybe and have a fixed size, even though it's different/larger size than any other rows, then the expand should be allowed only due to user dragging bottom-right corner of textarea to make it bigger, but smaller shouldn't be allowed and thus row itself too shouldn't allow smaller, for this textarea case.
//* allow the filter in /config to search the descriptions too
//* add description(s) for each config setting, visible in config.json as well, not just in webUI
//* allow multiple templates(eg. {file:X}) within a string, in config.json
//grok* Cert generation: generateCertIfNeeded — make sure private key is zeroed after use (defer runtime.KeepAlive or explicit zero).
//grok* WebUI password: bcrypt cost should be tunable + minimum 12+.
//* ensure consistent treatment of config settings when they come from webUI's save/apply(so in configHandler func.) and when from disk(loadMainConfig func.).
//* hide the upstream url from config.json maybe point to another file for it as it contains the ID used by nextdns for example and can't git commit that!
//  * get the NEXTDNS_ID from env or from cmdline? not cmdline, it appears in task manager!
//  * maybe use a pattern to reference a file with the current path where config.json is as the default implied path, like %id.key% which opens .\id.key file and puts that value in its place if %id.key% appears in the config.json
//  * or like: {file:id.key}
//  * ensure the save keeps the pattern not the result after pattern got replaced with actual.
//  * had undone a naive version of this 6 months ago, in commit 37ceffa6df9dbd04f43a96ad34df5391798c53b3
//* fixes for v0.12.6(minus a few commits:4? i moved tag since unpushed, it's this commit a6f47de774fa68eee7fe883b4d0d59859c882c1c) (todo:  done: 1,2,3,4,5 ) https://claude.ai/chat/8e5f83fe-cee6-48aa-9a31-c9483559918e
//* (this is 5 from above) replace panics except unreachables(actually do all) with something else like logFatal. Maybe even the unreachables so that they're logged first. Problem is that not everyone has logger, only ui. and s. !
//* ensure all defer happen in their own 'else' blocks, it's about code-correctness being enforced by code not by the coder when reading it with the intent of modifying it.
goodenough//* ensure all Unlock() or RUnlock() are happening in 'defer'-ers even if have to wrap it in anon(or better: helper) function calls, as long as it's safe.
//* make sure to check the return errors for things that i don't check for, filter Problems(in vscode) by: errcheck
//* in v0.11.3, do 3 from this: https://claude.ai/chat/e991c86f-3c0b-4d3e-8ea8-295c7defd419
//* make sure to check the return errors for like SetWriteDeadline
//* fix this: statsHandler builds RawBody with fmt.Sprintf and wraps it as template.HTML(body.String()). Right now the interpolated values (cfg.UpstreamURLsParsed, cfg.SNIHostnames, cfg.UpstreamIPs) are admin-set config, not attacker-controlled, so it's low risk — but it's the one spot in the codebase where "trust me, this is safe HTML" is asserted rather than enforced by the type system
//* see what happens if logged into webUI but u change pwd in config.json and Ctrl+R from console, what happens in webUI when nagvigating? should prompt for pwd after 1 fail, yes this happens
//* in the Config webUI page, the hash password Edit should be prompting for new password or something and compute the hash on the backend maybe.
//* ensure all files have //go:build windows  as the logic changes considerably if wanting to do it on non-Windows (eg. listening on same port twice, then tearing down the old one, is allowed on win11, not on Linux), but I never want to port this on Linux.
//* put a "return //unreachable" after every s.shutdown(x) so it's more obvious it doesn't return (funny)
//* handle the case when client(s) connect but don't do any DNS queries, maybe limit how many concurrents too!
//* add column sort to the webUI page for Response Blacklist IPs and for Hosts2IP
//* make the webUI page for response blacklist IPs be similar to /rules so that it has search and Edit buttons and added IPs are shown-added at the top of the list.
//* fix inconsistencies in v0.8.1 (intended: 2,6 done: 1,3,4,5): https://gemini.google.com/app/a782dd82602c19aa
//* make Ctrl+R relisten, so those TODO: Changes to ListenDNS, ListenDoH, ListenUI, or MaxConcurrentDNSTCPConns,
//* add more tests, lots more
//* make the whole config reloadable via Ctrl+R, not just the whitelist&blacklist.
//* change .bat to install golangci-lint.exe the way shadow.exe is installed, if not found.
//* let's add rate-limiting on login attempts (even basic in-memory) and log failed attempts with client PID/exe
//* ensure add/edit/delete (success or fail too) of rules/hosts etc. are s.logger-ed (many aren't at all currently)
//* check added rules to not allow hosts that aren't valid like "=accounts.google.com"
//* things to fix in v0.1.7 (done: 1,2,3,4,5): https://gemini.google.com/app/5ed11f7e85922bd6
//* make webUI entry for response blacklist IPs, like /rules and /hosts exists, note that /blocks is for recently blocked hosts quick-unblock
//* update the noscript to say the button for edit won't work without js, add/del do tho
//* fix: no log or message when trying to add a rule typ+pattern that already exists (regardless of Enabled value) even tho the code for it exists! must be the js code eating that 409 status from http.StatusConflict
//* must fix in 0.1.4 (done 1,2,3,5; todomeh: 4): https://chatgpt.com/g/g-p-69ef97a8577081919d895e95cb68f22d/c/6a3cd4f7-70fc-83eb-97a1-0df396962579
//* improvements for v0.1.2 (did 1,2,3,4,5):  https://gemini.google.com/app/0de925e7cdafd374 aka https://gemini.google.com/app/eff5f612c6d47b9e
//* state the value of newly added config.json key/defaults when logging it
//* invalidate cache for edited/delete whitelist/blacklist/hosts rules
//* Go's encoding/json makes a concrete choice: last value wins, silently. fix json.Decode to fail on dups!
//* see if I consistently strings.ToLower(strings.TrimSpace or wtw for rules/patterns in webUI and when loading from file.
//* fix rules with duplicated IDs existing in config and loaded at startup without complaints or purging
//* make the EDE reply optional, setable from config!
//* make ipv4 hints in HTTPS removable only if a config key is true (default) ie. add that key
//* add a config key that allows all HTTPS dns type queries but strips their IP hints. Currently it happens unconditionally!
//nonissuenow * see why Ctrl+Break key has weird behavior and needs 2 keys at press any key prompt and keeps the term in raw if terminate batch job is answered with Y ie. it's raw after our .exe exited!
//* add a config key which says all allowed A dns type queries also allow HTTPS ones
//* if the IP in listen_doh in config.json is changed, the cert needs to be regenerated to allow that IP to work, so need to delete cert.pem (not key.pem too) to cause regen of both .pem files with that IP setting.
//* show the IPs gotten from upstream in logs, even if cached, even if HTTPS dns type!(even tho stripped ipv4hint etc.)
//* make it 'pause' even when errors happen, else i need to wrap it in a .bat!
//* separate the whitelist and blacklist away from config.json because for example config.json may be marked as read-only (permissions wise) and the white/black list allowed to be modified!
//* fix rules being edited get duplicated (or overwrites old?)
//* deleting/adding rules via webUI shouldn't reorder them randomly
//* run both at build time:
  //(it's part of golancgi-lint)* get staticcheck in, see(private to me chat): https://chatgpt.com/c/695bbad2-99c8-832c-8876-e8a7219ae052
  //* look into getting github.com/golangci/golangci-lint
