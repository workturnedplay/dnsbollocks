todo:

* the log line "Dropping IP hint from the HTTPS reply" should tell me for what DNS query/host!

* maybe move fileWriter tests into wincoe since the impl. is there.

* ideally make the unblocks/reblocks (in Blocks(recent) page) more instant (currently 1+ sec per click, to finish loading) on the /blocks webUI page. Maybe i dno, do the fetch in the background(and change the Block/Unblock button to append text that says it's in progress) and if it fails keep the button as it were so it can be sent again, if it succeeds change it. Currently a full page reload seems to be happening on any 1 button click. I want to can do 3 button clicks in under 1 sec and they'd all be toggling their state as their fetch or POST or wtw succeeds, if it succeeds, if it fails (somehow let me know, a small text next to the right of the button? maybe even if it does succeed a green text?!). Since I'm hosting it on localhost IP and talking directly to in from win11, why does it take so long, does it bcrypt hash the password and compare it on every request and this is the cost that's causing such delays?

* do these for v0.15.2 (dontdo: 1_1 todo: 4_2(HttpOnly session cookie) done: the rest): https://gemini.google.com/app/277f8cbefd801877

* look for TODO and FIXME (not finished ones like doneTODO or nvmFIXME) and try to figure out how to fix.
* fixes/improvements for v0.10.0 (done:1_1, 3_1, 1_2, 1_3, 3_2, 4_2  todo: 4_1,  dontdo: 2): https://gemini.google.com/app/8bc2956c9fed71bb
* improvements for v0.0.6 (did 1,2,4,5,6; todo: 3) : https://gemini.google.com/app/8fba2ee635340715
* do more encapsulation for v0.1.1 (did ) : https://gemini.google.com/app/573dd1cd10f69c76
* link to Tinywall in readme and to Go lang to install Go.

nevermind/quirks(I don't wanna fix these):
//* currently: the new CIDR is still the old CIDR, I guess the wrong one is sent to the backend? in /response-blacklist page at least(?!) Actually, my bad it only happens for old_cidr=127.0.0.0/8 new_cidr=127.0.0.0/8  when my edited was 127.0.0.20/8 but this makes sense.
* if I Edit an entry(Stage it but not Apply it), then Delete it, the Undelete will recover the original one not the last edited value(s).
* if I filter and put the "!" after the word (even with a space) it acts as if it's !word so it excludes it.


no:
* make Edit button work without javascript? in webUI; Add/Delete already do work tho. This is too much work, not gonna do it because I'm not gonna use the program anyway since switching to Linux where I won't need it in this form.
//* sanitizeAndValidateConfig the defaultConfig() and i dno, use that or even panic2 BUG ? hmm, actually since sanitizeAndValidateConfig depends on defaultConfig() we can't do this.

half-done:
goodenough4now//* log.Debug that a .bak file was created, and on webUI on Apply, should probably say that too, like the whole filename .bak

done:
//* DRY some code in app.js
//* currently if I Add something (like a blacklisted IP/CIDR) it's staged (good) but then I Edit an existing one and as soon as I Stage this one, the newly added staged one from before is gone because of the current filter filtering it out, it shouldn't get filtered out, none of the staged ones should.
//* currently, if i Edit the same thing multiple times(ie. Edit, change it, Stage), the number of changes increases, but shouldn't, it's same row.
//* I need per-row Discard buttons (so Stage, Cancel, Discard) which will discard all staged edits(ie. no matter how many times I pressed Edit on this row and changed it) to the state it was before anything begun to be staged.
//* maybe detect when Staging a row that has no changes compared to unstaged variant of it and then don't mark it as staged and if it was staged(from a prev. Edit of it) and now it isn't anymore(after this Edit, due to same contents as unstaged/before one) ensure the counter is properly decremented to acount for this unstaging of it.
//* also need to be able to Undelete a row that I stage-deleted, so this means the filter must still see it after deletion and it now has a button to Undelete it.
//* make Add/Delete to also stage changes, for the 3 pages (not for /config): /rules /hosts /response-blacklist  Currently only Edit button stages changes.
//* instead of rt.LogMgr.Get().Error(...) we should have rt.LogMgr.Error(...) (so a thin wrapper to it), or maybe even rt.Error(...) or nah(it implies Runtime.Error() not logger.Error(), no?), the fomer.
//* find out why say on /response-blacklist page if I Edit, Stage, Discard All _sometimes_ won't reload page (since it does that location.reload()) at all, not even in Console (F12 in firefox) running "location.reload()" doesn't reload page, says undefined (return value? possibly normal) - might be a firefox 152.0.5 bug? there are no errors from script in Console btw.
//* make Discard All button in /config also ask for confirmation before navigating away (aka location.reload()) just like say /response-blacklist page does.
//* on /rules page the Stage button will create a new non-editing line and still remain itself in Edit mode! also doesn't show the banner that there are staged things and how many, so no Apply or Discard
//* on /hosts Stage button does nothing, i forgot a line
//* make the applying of all staged changes happen in 1 POST not 1 per each change and if any fails stop.
//* allow multiple rows(like from /rules /hosts and /response-blacklist but not /config which already has this feature) to be in Edit mode and if more than one, the Save button should transform into Stage and thus don't apply immediately when pressed (just like in /config page), the Apply should happen only when the global Apply button (which would appear, similar to how /config does it) is pressed (or Cancel if cancelled).
//* also decouple NewServer and config.json loading so that when NewServer is called the config is already not the defaults which affects the in-NewServer inits like: s.fileWriter = wincoe.NewWin11SafeFileWriter(...)
//* didn't I remove a janitor function recently? from what was that? it wasn't the cache store 's janitor lookslike. Was there another one then? ah yes, it was ClientRateLimiter's janitor which was set to 10 hardcoded minutes.
//* ensure sanitizeAndValidateConfig doesn't use the s.fileWriter, if it doesn't, then apply the loaded config to s.fileWriter after sanitizeAndValidateConfig is ran in loadMainConfig
//* Do you want to extract the retry limits/backoffs inside the SafeWriteFile function (tryTimesForFileOp = 6 / backoffMsTimeForFileOpPerTry = 100) into the config as well, or do you feel those are low-level engine constants better left hidden from the end user?
//* maybe add: !TERM to mean exclude that from the filtered(in any webUI pages that support filtering), example filter string: face | fbcdn !hugging
//* do these for v0.15.2 (dontdo: 1_1 done: 2_1, 2_2, 3_1, 3_2, 4_1, 1_2(ReplaceFileW) todo: 4_2): https://gemini.google.com/app/277f8cbefd801877
//* make the filters have AND and OR between terms, somehow, but using & and | perhaps and make a note somewhere(maybe same place when filter is empty) that they're possible.
//* do the 1_2(ReplaceFileW) from below as an addition to what already exists. See if to manually make a .bak if doing it with ReplaceFileW but it fails.
//* continue the 4_1 from here: https://claude.ai/chat/47de6072-51ed-4c00-9862-40980783e1c3
//* Exposing TLSCertFile and TLSKeyFile in the config allows users to mount valid internal CA certificates. You would run these through your existing checkAndClean file logic in sanitizeAndValidateConfig.
//* see what other values should be in config.json or should be clamped/changed depending on other values, i'm looking at you buildSet with those t2.ReadIdleTimeout and t2.PingTimeout and KeepAlive in net.Dialer, at least.
//* continue the tests in https://claude.ai/chat/c3a10bf7-2d3a-4655-8f2e-c9a54e57b405  tests are written to a file but 'Failed to load file content' "Try Again" button, or "Download as Go" (says failed) or "Copy" also won't work. F12 in firefox says "503 Service Unavailable" and response payload is "upstream connect error or disconnect/reset before headers. reset reason: connection timeout", ok it loaded a few mins later and i did Copy (download didn't work still)
//* LoggerManager(I started the first step about this already): the logger should be decoupled from Server but since Server should be able to change the log file then it should be able to close the old file, but it should do this by telling the logger to do it. So don't like accept 2 args like func NewServer(logger *slog.Logger, logFile *os.File, ...  but rather 1 arg that represent the logger (which was inited in OldMain already, to full logger after config.json got loaded) then Server can tell logger it wants a new file and logger knows to close old and open new, also logger should have a Close() for when OldMain is done with it or something. But also, logger should can be closed in tests, to then allow tests to delete the tmpdir they made.* decouple fileWriter from Server and pass its instance to all that use it like saveConfig, though loads also use it to check for powerloss file (and they can invoke save too)
//* saveConfig should be decoupled from Server ! but since it's using Server.fileWriter.SafeWriteFile() and this one has its own settings, then hmmm, how shall we do this? pass the instance to it? and if nil instance then it will spin one with defaultConfig() defaults?! sounds reasonable to me, tho this means the fileWriter instance itself is decoupled from Server too, and thus passed to the server and Server should can reload it on Ctrl+R with diff. args, so I guess that means just setting the 2 things it has (retries and backoff ms), this might also mean a good idea to decouple loadDependentStores from Server and each of those sub functions it uses(loadQueryWhitelist, loadResponseBlacklist, loadLocalHosts) which means those funcs get a fileWriter instance passed to them because they each can save and their save funcs need it too.
//* should prolly decouple the os.Exit from Server.shutdown(exitCode) or use something else to shutdown/close server without exiting, so it can be used from tests, and also somehow undo the log file set/promoted, get back to the original, or somehow close it so tmpdir can be deleted from tests. But need to analyze the side-effects of this, as there's code that used to depend on s.shutdown(N) meaning os.Exit, and the general logic/arch. of the whole thing may be dependent on this.
//* maybe decouple fileWriter better, so it's inited in OldMain and everything else uses its instance, like LoadAndValidateConfig (which currently spins up a default one inside it, temporarily to check for .powerloss file) and NewServer(which currently get a nil thus spins up a new one with the loaded config's values) and that's it I guess, srv.SaveConfig does use the proper one that NewServer spun up.
//* ensure LoggerManager is inited outside of NewServer, so it's decoupled from it. Server can "talk" to it if it needs to log to a new file, or change to a different loglevel, but shouldn't have to worry about doing the closing of old logs for example, that's LoggerManager's business. Thus lots of logic from initFullLogging shouldn't be part of Server but part of LoggerManager perhaps.
//* (the how for the above two TODOs) Introduce a Runtime struct for long-lived process services (LoggerManager, FileWriter, etc.). OldMain should own and initialize it, Reload() should update it, and NewServer should receive it as a dependency rather than constructing those services.
