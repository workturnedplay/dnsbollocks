todo:
* we should have a config.json setting/key which when true (the default) would require the host from /hosts (in webUI) to be in whitelist akak /rules (in webUI) first before accepting/resolving it as a host to IP mapping. Hmm, I'm kinda thinking maybe we should just make the code assume it's false thus never require it to be in whitelist also, because whitelist may become stale if I change the hostname/pattern in /hosts then whitelist would allow the old one to be forwarded which is very bad! So yes, let's do this then, no setting, but change the code so it doesn't require whitelisting the host(well any of them, oc) from /hosts first. So after doing this, then we don't have to allow any domain(except those in /hosts ) to get their _port._https. prefixed domains resolved if they're DNS HTTPS type, which is what currently happens.

* this is probably the wrong way to do this? "// Modern browsers(eg. Firefox 152.0.6) query _<port>._https.<domain> when using non-standard ports.
	// We strip this prefix so it matches against the base domain in rules and hosts." Or, if it's correct, let me know, if not, what's the better/best way?
  
* the []string-textarea auto-height code persists its custom height to a single shared sessionStorage key (config_textarea_height) rather than one per field, so if you resize two list-type textareas open at once, they'll fight over that one stored height.

* in blockResponse some returns(or just the last one then?) modify the request and return it, it should return a newly allocated one based on the reqMsg like servfailResponse and formerrResponse already do!

* check if after a Reload/applyConfig, any cached stuff like a 'log' var is being used when it may actually be closed and how to fix this? never cache them maybe? and/or maybe delay closing of the old logs, or should I never cache logs and just get the live one and use it upon each logging attempt!? because currently I'm still getting one "[asyncLogWriter "dnsbollocks.log"] WARNING: log "was closed"; 1 line(s) dropped so far" at shutdown, but only if I did a Reload() first!

* maybe move fileWriter tests into wincoe since the impl. is there.

* do these for v0.15.2 (dontdo: 1_1 todo: 4_2(HttpOnly session cookie) done: the rest): https://gemini.google.com/app/277f8cbefd801877

* look for TODO and FIXME (not finished ones like doneTODO or nvmFIXME) and try to figure out how to fix. See this for v0.21.0: https://gemini.google.com/app/d9d0f04a416f0b41 from "Safety & Edge Cases" onwards.

* fixes/improvements for v0.10.0 (done:1_1, 3_1, 1_2, 1_3, 3_2, 4_2  todo: 4_1,  dontdo: 2): https://gemini.google.com/app/8bc2956c9fed71bb
  4_1 is this: Client-Side Filtering on Large Data: In applyRulesFilter and applyBlacklistFilter, you iterate through all <tr> elements in the DOM. If a user uploads a blacklist of 50,000 IPs, rendering all those rows and filtering them in JS will completely freeze the browser. Fix: We should implement server-side pagination or virtual scrolling if the list exceeds a few hundred items.
* improvements for v0.0.6 (did 1,2,4,5,6; todo: 3) : https://gemini.google.com/app/8fba2ee635340715
* do more encapsulation for v0.1.1 (did ) : https://gemini.google.com/app/573dd1cd10f69c76
* link to Tinywall in readme and to Go lang to install Go.

nevermind/quirks(I don't wanna fix these):
//* currently: the new CIDR is still the old CIDR, I guess the wrong one is sent to the backend? in /response-blacklist page at least(?!) Actually, my bad it only happens for old_cidr=127.0.0.0/8 new_cidr=127.0.0.0/8  when my edited was 127.0.0.20/8 but this makes sense.
* if I Edit an entry(Stage it but not Apply it), then Delete it, the Undelete will recover the original one not the last edited value(s).
* if I filter and put the "!" after the word (even with a space) it acts as if it's !word so it excludes it.

no:
* make Edit button work without javascript? in webUI; Add/Delete already do work tho. This is too much work, not gonna do it because I'm not gonna use the program anyway since switching to Linux where I won't need it in this form.
//* sanitizeAndValidateConfig the defaultConfig() and i dno, use that or even panic2 BUG ? hmm, actually since sanitizeAndValidateConfig depends on defaultConfig() we can't do this.
* 3 "UI State Gap: Wildcard Rules vs. Recent Blocks Button State" from https://gemini.google.com/app/e6c6d356d495a825
* improvements: I could probably cache the conversion from "{builtin:clientexe}" to the requesting exe, in doSingleDoHRequest(), so that I don't do it everytime, actually it may cost more than the PathEscape i'm trying to cache, hmm. nevermind.

half-done:
goodenough4now//* log.Debug that a .bak file was created, and on webUI on Apply, should probably say that too, like the whole filename .bak

done:
//* to fix (todo:  done: 1C,1D,2A  dontdo: 1B,3C notathing: 1A,2B,3A,3B,3D) from https://gemini.google.com/app/f0690efe8f767ec7 via Gemini 3.5 Thinking (many false positives, but ofc!)
//* need a way to say (and disable the Apply button) during the fetch (sending of the stuff to the backend) that it is doing it(maybe temp-rename the Apply into 'in progress' or something), because in case it doesn't reply for 1 minute, for wtw reason, I shouldn't be able to press Apply again to cause another fetch which would then reload the page while the prev. one says it failed in an alert but you can't read the alert due to page got reloaded automatically but the second succeeding fetch (presumably that's why it got reloaded)! Also/or maybe the second one gets this "Conflict: config.json was modified on disk since you loaded this page." reply if I actually submitted a request from a stale page (ie. i restarted the program since), and it's trying to tell me that in an alert but I can't see it because it reloads page and gets rid of the alert. I know it hits postAdminForm in that "catch" because the console.log says "Failed to apply configuration network error: TypeError: NetworkError when attempting to fetch resource." (but this may be for the first case). Btw, if I do restart it, portmaster seems to need about 30sec or more to allow the newly started exe (even tho it's same .exe and path and has the rules already, because it's same profile/rules that applied for the old exe) thru the firewall, that's why after restart it would hang and eventually timeout (which is why the NetworkError happened above).
//* in /config page of webUI, currently there can't be more than 1 Edit in progress (but it should can!), unlike for others like /rules where multiple Edit(s) can be open at same time!
//* find out why I get the following 2 log lines(well, the last one) during Reload() but only if something changed in the config.json, and let's fix it so it doesn't drop any lines?:
//  INFO 2026-07-20 11:55:14.107414300+02:00 CEST Config reload complete. Listeners, cache, and connection limits rebound as needed.
//  [asyncLogWriter "dnsbollocks.log"] WARNING: log "was closed"; 1 line(s) dropped so far (underlying sink is not keeping up, e.g. due to slow/contended disk)
* do fixes for v0.27.0 (todo:  done:1,2  dontdo: 3(UI State Gap: Wildcard Rules vs. Recent Blocks Button State)  invalid: 4) see: https://gemini.google.com/app/e6c6d356d495a825
//* fixes todo for v0.26.4 (todo: done: 1,2,3) see: https://gemini.google.com/app/1c55a5851c12fb33
//* need to make the logic within updateInnerState be part of sanitizeAndValidateConfig, if that makes more sense! Maybe to avoid some kind of half-state race during Reload()
//* so I'm doing "if resp.Rcode != dns.RcodeSuccess" then I respond with servfailResponse(msg), that's bad according to '1' in the below gemini url
//no,fixed//* is it ok to have servfailResponse(msg) modify msg's fields inplace, or should it return a new one based on msg? and why?
//  * do the new for blockResponse too because if servfailResponse and formerrResponse also do new, then blockResponse should too? in the second branch.
